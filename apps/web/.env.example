# API base URL — used by better-auth client to talk to the backend
# Dev: http://localhost:3001 (proxied via Vite)
# Production: https://api.compsphere12.id (direct browser → API)
VITE_API_URL=http://localhost:3001
